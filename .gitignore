node_modules/
dist/
build/
.tmp/

# yt-dlp binary is fetched by scripts/fetch-ytdlp.sh (~36MB, goes stale fast)
bin/

# Windows tray and application icons are generated on install. The macOS menu
# bar templates are supplied artwork and must remain in the repository.
assets/*.png
!assets/trayTemplate.png
!assets/trayTemplate@2x.png

*.log
# personal data - lives in the app's userData dir, never in the repo
sessions.json
sessions.json.tmp
playlists.json
playlists.json.tmp
settings.json
settings.json.tmp
.DS_Store

# stamped at pack time by scripts/build-info.js
build-info.json
